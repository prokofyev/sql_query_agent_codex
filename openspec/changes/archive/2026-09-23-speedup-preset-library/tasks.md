## 1. Модель пресета

- [x] 1.1 Убрать из `PresetQuery` в `src/sql_query_agent/presets.py` поля ожидаемого эффекта и команды индекса, а также перечисление эффектов и вспомогательные выборки по эффекту; проверить, что модуль импортируется и `uv run ruff check src` проходит
- [x] 1.2 Убрать поле ожидаемого эффекта из `PresetSchema` и его заполнение в `src/sql_query_agent/api/schemas.py`; проверить тестом ответа `/presets`, что поля `expected` в ответе нет, а `note` есть

## 2. Библиотека из 12 запросов

- [x] 2.1 Собрать 12 пресетов: точечный счётчик, страница с сортировкой, `IN`-список с группировкой, фильтр по двум условиям, агрегат по размеру, соединения двух и трёх таблиц, оконные функции `row_number`, `count(*) over` и топ-N на товар; каждому дать подпись и пояснение
- [x] 2.2 Проверить тестом `tests/test_presets.py`, что в библиотеке 12 запросов, все проходят проверку ввода как одиночный запрос на чтение, идентификаторы уникальны, у каждого есть подпись и пояснение, и ни у одного нет команды индекса
- [x] 2.3 Обновить проверки библиотеки в `tests/test_api.py` и `tests/test_ui.py` под новый размер и состав; проверить, что подписи непусты

## 3. Сквозные и интеграционные сценарии

- [x] 3.1 Добавить интеграционный тест, который для каждого пресета на реальной базе применяет индекс по `sku.product_id` и требует значимого ускорения; проверить прогоном с маркером `integration`
- [x] 3.2 Перевести `tests/test_schema_invariance_integration.py` на константу индекса в тесте вместо команды из пресета; проверить, что схема `public` остаётся неизменной после прогона всех пресетов
- [x] 3.3 Обновить `tests/test_end_to_end.py`: заменить идентификаторы пресетов на новые, а сценарий «без ускорения» перевести на свободный ввод; проверить прогоном с маркером `integration`

## 4. Документация

- [x] 4.1 Убрать из `README.md` утверждение, что библиотека содержит случаи, где индекс не помогает, и описать новый состав библиотеки; проверить, что формулировка не противоречит спецификации `query-intake`

## 5. Проверка

- [x] 5.1 Прогнать `uv run pytest -q` и убедиться, что быстрые тесты проходят
- [x] 5.2 Прогнать `uv run pytest -q -m integration` и убедиться, что интеграционные тесты проходят на локальной базе
- [x] 5.3 Прогнать `uv run ruff check src tests` и убедиться в отсутствии замечаний
