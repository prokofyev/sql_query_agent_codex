## 1. Модель экрана

- [x] 1.1 Добавить в `RunView` признак «исправление принято» и свойство показа карточки исправления;
  заполнять признак в `build_run_view` из решений отчёта; покрыть тестами в `tests/test_ui.py`
- [x] 1.2 Показывать карточку в `ui/app.py` по новому свойству вместо `bool(view.fixed_sql)`

## 2. Ход обработки после решения

- [x] 2.1 Показывать `pending_view` со статусом «Обработка запроса» в `run_decision` до ответа сервера;
  передавать текст запроса из страницы
- [x] 2.2 Тесты: после принятия исправления карточка скрыта, при отказе видна, во время обработки
  решения статус — «Обработка запроса»

## 3. Проверка

- [x] 3.1 Прогнать `uv run pytest -q` и `uv run ruff check src tests`
