## MODIFIED Requirements

### Requirement: Подбор похожих имён

Для каждого ненайденного имени инструмент SHALL подбирать похожие существующие имена и возвращать их
вместе с оценкой схожести. Поиск MUST выполняться по данным, полученным из `information_schema`.
Если ни один кандидат не проходит порог схожести, инструмент SHALL помечать это имя как ненайденное
без кандидатов и MUST NOT подставлять замену.

#### Scenario: Опечатка в имени колонки
- **WHEN** инструмент получает имя колонки, которого нет в таблице
- **THEN** он возвращает существующие имена этой таблицы, похожие на введённое
- **AND** для каждого кандидата указана оценка схожести

#### Scenario: Похожих имён не найдено
- **WHEN** для ненайденного имени не найдено ни одного кандидата выше порога схожести
- **THEN** инструмент возвращает это имя среди ненайденных
- **AND** список кандидатов для него пуст

### Requirement: Однократное исправление запроса

Модель SHALL один раз исправить исходный запрос, опираясь на предложенных кандидатов, и только если
для каждого ненайденного имени есть хотя бы один кандидат на замену. Модель MUST NOT придумывать
имена, которых нет среди предложенных кандидатов. Повторная проверка исправленного запроса MUST NOT
запускаться автоматически.

#### Scenario: Модель исправляет опечатки
- **WHEN** инструмент вернул ненайденные имена и для каждого из них есть кандидаты на замену
- **THEN** модель возвращает исправленный текст запроса
- **AND** показывает пользователю, какие имена были замены

#### Scenario: Исправление не повторяется автоматически
- **WHEN** модель выдала исправленный запрос
- **THEN** система не запускает ещё один цикл исправления
- **AND** решение передаётся пользователю

#### Scenario: Исправление не выполняется без кандидатов
- **WHEN** хотя бы для одного ненайденного имени нет кандидатов выше порога схожести
- **THEN** модель не вызывается для исправления
- **AND** исходный запрос не изменяется

## ADDED Requirements

### Requirement: Сообщение о ненайденных именах без замен

Система SHALL сообщать пользователю, какие именно имена не найдены и что подходящих замен нет.
Для каждого ненайденного имени сообщение MUST указывать, о чём речь — таблица или колонка, — и само
имя ровно в том виде, в каком оно записано в запросе; для колонки MUST быть указана таблица, в
которой её искали. Система MUST NOT показывать исправленный запрос или список замен, если замены не
предлагались.

#### Scenario: Не найдена таблица без похожих названий
- **WHEN** таблица из запроса отсутствует в схеме и кандидатов выше порога схожести для неё нет
- **THEN** пользователю сообщается, что такая таблица не найдена
- **AND** в сообщении указано имя таблицы так, как оно записано в запросе
- **AND** пользователю сообщается, что подходящих замен нет

#### Scenario: Не найдена колонка без похожих названий
- **WHEN** колонка из запроса отсутствует в таблице и кандидатов выше порога схожести для неё нет
- **THEN** пользователю сообщается, что такая колонка не найдена
- **AND** в сообщении указаны имя колонки и таблица, в которой её искали

#### Scenario: Ненайденных имён несколько
- **WHEN** в запросе несколько имён без кандидатов
- **THEN** сообщение перечисляет все такие имена
- **AND** по каждому из них указано, таблица это или колонка

### Requirement: Завершение сессии при отсутствии замен

Если хотя бы одно ненайденное имя осталось без кандидатов, система SHALL завершать обработку запроса
на этом шаге без подтверждения исправления пользователем и без перехода к замеру. Результат обработки
MUST содержать сообщение о ненайденных именах и MUST NOT содержать предложение об исправлении или об
индексе.

#### Scenario: Сессия завершается без подтверждения
- **WHEN** среди ненайденных имён есть хотя бы одно без кандидатов
- **THEN** обработка завершается
- **AND** пользователю не предлагается принять или отклонить исправление
- **AND** замер запроса не выполняется
