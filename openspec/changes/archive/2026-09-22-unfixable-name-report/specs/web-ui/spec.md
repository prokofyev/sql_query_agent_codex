## ADDED Requirements

### Requirement: Отображение ненайденных имён без замен

Интерфейс SHALL показывать сообщение о том, какие имена не найдены и что подходящих замен нет, когда
проверка имён завершилась без кандидатов. Интерфейс MUST NOT показывать карточку исправления и
элементы «принять»/«отказаться», если исправление не предлагалось. Сообщение MUST показываться как
результат проверки имён, а не как сбой обработки.

#### Scenario: Показано сообщение о ненайденных именах
- **WHEN** прогон завершился из-за отсутствия замен для ненайденных имён
- **THEN** интерфейс показывает сообщение с перечислением ненайденных имён
- **AND** в сообщении указано, что подходящих замен нет
- **AND** предложение об исправлении и действия по нему не отображаются

#### Scenario: Возврат к вводу нового запроса
- **WHEN** сессия завершилась из-за отсутствия замен
- **THEN** поле ввода запроса снова доступно
- **AND** пользователь может отправить новый запрос без перезагрузки страницы
