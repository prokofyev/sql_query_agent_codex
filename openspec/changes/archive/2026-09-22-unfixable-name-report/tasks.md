## 1. Домен и проверка имён

- [x] 1.1 Добавить в `src/sql_query_agent/domain.py` признак исправимости результата проверки имён (ненайденные имена без кандидатов) и проверить юнит-тестом, что он вычисляется из `unknown` и не требует обращения к базе
- [x] 1.2 Добавить в `src/sql_query_agent/tools/schema_check.py` чистые функции: «есть ли имя без кандидатов» и «исправимы ли все ненайденные имена»; проверить юнит-тестами случаи «все с кандидатами», «часть без кандидатов», «все без кандидатов»
- [x] 1.3 Реализовать сборку сообщения о ненайденных именах без замен по списку `UnknownName` (таблица — «таблица X», колонка — «колонка Y таблицы Z», плюс фраза об отсутствии подходящих замен); проверить юнит-тестами форматы для таблицы, колонки и нескольких имён, а также отсутствие имён, которых нет во входных данных
- [x] 1.4 Убедиться, что список кандидатов для имени без совпадений остаётся пустым и замены не подставляются; проверить тестом с порогом выше любого совпадения

## 2. Граф агента

- [x] 2.1 Расширить маршрутизатор `needs_fix` в `src/sql_query_agent/agent/nodes.py`: ветка исправления выбирается только когда исправимы все ненайденные имена, иначе маршрут ведёт к терминальному узлу; проверить юнит-тестами все три исхода маршрутизации
- [x] 2.2 Реализовать терминальный узел (завершение без исправления), который кладёт в состояние сообщение, статус и не запускает ни модель, ни замер; проверить тестом, что модель исправления не вызывается (`FakeModel.fix_calls == 0`) и предложение об индексе не формируется
- [x] 2.3 Запретить вызов модели исправления, если кандидатов нет хотя бы у одного имени, и зафиксировать это в промпте `FIX_SYSTEM_PROMPT` в `src/sql_query_agent/agent/llm.py`; проверить тестом, что модель не вызывается в этом случае
- [x] 2.4 Обновить сборку графа `src/sql_query_agent/agent/graph.py` под новую ветку и проверить, что для исправимого запроса прежний путь (`prepare_fix → confirm_fix → measure`) не изменился

## 3. Отчёт и статус прогона

- [x] 3.1 Добавить в `RunStatus` (`src/sql_query_agent/run/report.py`) терминальный статус «имена не найдены, замен нет» и проверить юнит-тестом, что он терминален
- [x] 3.2 Добавить в `RunReport` поле с сообщением о ненайденных именах и заполнять его из состояния прогона; проверить тестом, что для исправимого прогона поле пусто
- [x] 3.3 Научить `_status` относить новый исход к терминальному результату, а не к `FAILED`, и не подставлять `error`; проверить тестом, что `status` нового исхода не `failed` и `error is None`
- [x] 3.4 Проверить, что новый исход не порождает `FixProposal`/`IndexProposal` в отчёте; проверить тестом построения отчёта

## 4. API и веб-интерфейс

- [x] 4.1 Добавить сообщение о ненайденных именах в `RunReportSchema` и передавать его в ответе `src/sql_query_agent/api/schemas.py`; проверить тестом ответа API
- [x] 4.2 Пробросить сообщение через `src/sql_query_agent/ui/client.py` в собираемый отчёт; проверить тестом клиента против тестового приложения
- [x] 4.3 Показать сообщение в `RunView` и `src/sql_query_agent/ui/app.py`: отдельный блок с текстом и статус прогона; проверить тестом `build_run_view`, что текст попадает в модель экрана
- [x] 4.4 Скрыть карточку исправления и кнопки «принять»/«отказаться» для нового исхода и вернуть доступность поля ввода; проверить тестом вью, что `fixed_sql is None`, список замен пуст, а форма доступна

## 5. Журнал и метрики

- [x] 5.1 Убедиться, что прогон с новым статусом пишется в журнал как завершённый, с ненайденными именами и без ошибки; проверить тестом `record_from_report`
- [x] 5.2 Проверить, что метрика прогонов учитывает новый статус, а гистограмма длительности замеров не получает значений; проверить юнит-тестом `observe_run`

## 6. Сквозная проверка

- [x] 6.1 Прогнать сквозной сценарий на подставных модели и базе: запрос с опечаткой без похожих названий → сообщение о ненайденных именах → сессия завершена → поле ввода доступно; проверить тестом `tests/test_end_to_end.py`
- [x] 6.2 Прогнать сценарий с именем без кандидатов на реальной базе (маркер `integration`): проверить, что обращений к модели исправления нет, замер не выполняется и сообщение содержит имя из запроса
- [x] 6.3 Проверить сквозной сценарий с реальной моделью за флагом `--run-llm`: запрос с опечаткой и кандидатом по-прежнему приводит к предложению исправления и подтверждению
