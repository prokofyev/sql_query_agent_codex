## 1. Извлечение идентификаторов

- [x] 1.1 Заменить структуру аргумента инструмента `check_schema` на таблицы, алиасы и колонки как
  записаны; обновить модели в `domain.py` и схему аргумента в `check_schema.py`; проверить
  `uv run pytest -q tests/test_domain.py tests/test_check_schema_tool.py`
- [x] 1.2 Переписать `EXTRACTION_SYSTEM_PROMPT`: модель возвращает таблицы, алиасы и колонки, не
  определяет принадлежность колонки таблице; проверить извлечение на шести формах запроса из
  `tests/test_llm_end_to_end.py`
- [x] 1.3 Обновить фикстуры модели в `tests/fakes.py` под новую структуру; проверить
  `uv run pytest -q tests/test_agent_graph.py`

## 2. Разрешение принадлежности колонок

- [x] 2.1 Добавить в `db/catalog.py` поиск таблиц, содержащих колонку; покрыть тестом в
  `tests/test_domain.py`
- [x] 2.2 Реализовать в `tools/schema_check.py` детерминированное определение владельца: квалификатор
  с разворотом алиаса, затем единственная таблица запроса, содержащая колонку; покрыть сценариями
  «колонка принадлежит другой таблице запроса» и «квалифицированная колонка» в
  `tests/test_schema_check.py`
- [x] 2.3 Изменить поиск кандидатов для ненайденной колонки: пул — колонки всех таблиц запроса;
  проверить, что для запроса пользователя из `proposal.md` ненайденных имён нет
- [x] 2.4 Проверить отсутствие ложных срабатываний на всех пресетах:
  `uv run pytest -q -m integration tests/test_check_schema_integration.py tests/test_presets.py`

## 3. Неоднозначные колонки

- [x] 3.1 Добавить признак неоднозначности в результат проверки и список таблиц, где колонка есть;
  покрыть тестом в `tests/test_schema_check.py`
- [x] 3.2 Сделать неоднозначную колонку исправимой, чтобы прогон уходил в `prepare_fix`; проверить
  маршрутизацию в `tests/test_agent_graph.py`
- [x] 3.3 Дополнить `FIX_SYSTEM_PROMPT` требованием квалифицировать неоднозначную колонку именем
  таблицы из схемы; проверить исправление на запросе с `brand_id` из `proposal.md`
- [x] 3.4 Убедиться, что отчёт и интерфейс не меняются: `uv run pytest -q tests/test_run_report.py
  tests/test_ui.py`

## 4. Проверка на реальной базе

- [x] 4.1 Прогнать сценарий `select brand_name, product_name from product join brand on
  brand.brand_id = product.brand_id`: проверки проходят, исправление не предлагается, замер
  выполняется
- [x] 4.2 Прогнать сценарий `select brand_id, product_name from product join brand on
  brand.brand_id = product.brand_id`: прогон останавливается на подтверждении исправления с
  квалификатором и не доходит до замера с ошибкой
- [x] 4.3 Полный прогон тестов: `uv run pytest -q` и `uv run ruff check src tests`
