## 1. Перенос принятого исправления в поле ввода

- [x] 1.1 Добавить в `src/sql_query_agent/ui/view.py` чистую функцию, возвращающую текст для поля ввода по показанной модели и принятому решению: при принятии исправления — исправленный запрос, в остальных случаях — прежний текст; проверить юнит-тестом в `tests/test_ui.py` все три случая (принятие исправления, отказ, решение по индексу)
- [x] 1.2 Применить функцию в обработчике решения `src/sql_query_agent/ui/app.py`: хранить последнюю показанную модель в состоянии страницы и подставлять текст в `sql_input` до обращения к серверу; проверить тестом, что после принятия исправления поле содержит исправленный запрос, а после отказа — прежний

## 2. Немедленное скрытие кнопок решения

- [x] 2.1 В обработчике решения `src/sql_query_agent/ui/app.py` скрывать ряд решений сразу при выборе пользователя, до запроса к серверу; проверить, что `render` по-прежнему задаёт видимость по модели состояния, поэтому для нового предложения (индекса) кнопки возвращаются
- [x] 2.2 Проверить тестом `tests/test_ui.py`, что кнопки решения не остаются видимыми для закрытого решения и доступны для следующего предложения

## 3. Проверка

- [x] 3.1 Прогнать быстрые тесты `uv run pytest -q` и убедиться, что все проходят
- [x] 3.2 Прогнать `uv run ruff check src tests` и убедиться в отсутствии замечаний
