## 1. Схема в файле и настройка

- [x] 1.1 Добавить настройку `schema` с полем `path` (по умолчанию `resources/schema.yaml`) в `config.py` и проверить, что значение читается из `SQA_SCHEMA__PATH` тестом настроек.
- [x] 1.2 Написать `resources/schema.yaml` с подписью и рисунком демо-схемы в псевдографике: таблицы `brand`, `product`, `sku`, `product_size`, `product_color` со столбцами, связями и числом строк из текущей демо-базы, и проверить, что файл читается загрузчиком без ошибок.
- [x] 1.3 Добавить модуль загрузки схемы с моделью «подпись + рисунок» и функцией загрузки: отсутствующий или пустой файл даёт «схемы нет», повреждённый — понятную ошибку; проверить тестами на файле, пустом файле и файле без рисунка.

## 2. Эндпоинт схемы

- [x] 2.1 Добавить `GET /schema`, отдающий подпись и рисунок из файла; проверить тестом, что записи берутся из файла, заданного настройкой.
- [x] 2.2 Проверить тестами ветки «файл отсутствует или пуст» (пустой ответ `200`) и «файл повреждён» (ошибка `schema_unavailable`).

## 3. Панель в интерфейсе

- [x] 3.1 Добавить функцию клиента интерфейса для схемы и чистую функцию модели экрана, собирающую панель из подписи и рисунка; проверить тестами на заполненной и пустой схеме.
- [x] 3.2 Показать панель схемы вверху страницы, перед полем ввода, скрывая её при пустой схеме; проверить тестом смонтированной страницы, что схема и подпись присутствуют в разметке.

## 4. Документация и проверка

- [x] 4.1 Обновить `README.md` (раздел о схеме в интерфейсе и о происхождении числа строк) и `.env.example` (настройка `SQA_SCHEMA__PATH`).
- [x] 4.2 Прогнать `uv run pytest -q` и `uv run ruff check src tests`, убедившись, что быстрые тесты зелёные и линтер чист.
