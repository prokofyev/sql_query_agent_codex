## 1. Пресеты в YAML

- [x] 1.1 Добавить настройку `presets` с полем `path` (по умолчанию `resources/presets.yaml`) в `config.py` и проверить, что значение читается из `SQA_PRESETS__PATH` тестом настроек.
- [x] 1.2 Написать `resources/presets.yaml` с 12 пресетами по составу из `design.md` и проверить, что файл читается загрузчиком без ошибок.
- [x] 1.3 Переписать `presets.py`: загрузка списка из YAML вместо кортежа `PRESET_QUERIES`, с валидацией обязательных полей `id`/`title`/`sql` и понятной ошибкой при повреждённой записи.
- [x] 1.4 Реализовать поведение «файл отсутствует или пуст»: пустой список без исключения; проверить тестом на несуществующий путь.
- [x] 1.5 Переключить `api/routes.py` на загрузчик из настроек и проверить, что `GET /presets` отдаёт записи из файла тестом.
- [x] 1.6 Проверить, что пустая библиотека не ломает `GET /presets` и интерфейс: тест на пустой ответ и на открытие страницы без пресетов.

## 2. Пересборка библиотеки пресетов

- [x] 2.1 Заполнить `resources/presets.yaml` запросами по товару, размеру, дате и бренду согласно таблице в `design.md`, исключив фильтры по низкоселективному `product_color_id`.
- [x] 2.2 Обновить `tests/test_presets.py` под источник из файла: число запросов, уникальность `id`, наличие подписи и пояснения, отсутствие команды создания индекса, покрытие форм выборки (включая диапазон).
- [x] 2.3 Проверить тестом, что библиотека содержит запросы, ускоряемые разными колонками, а не одним рычагом.

## 3. Удаление тестов, привязанных к конкретной базе

- [x] 3.1 Удалить `tests/test_preset_speedup_integration.py` и убедиться, что прогон остаётся зелёным.
- [x] 3.2 Убрать из `tests/test_apply_index_integration.py` проверки счётчиков строк (102/1000/100000) и низкоселективного `product_color_id`, оставив проверки отката индекса и обработки ошибок DDL.
- [x] 3.3 Убрать привязку к колонкам демо-схемы из `tests/test_end_to_end.py` и `tests/test_journal_integration.py`, проверив, что сквозной цикл и журнал тестируются на любой доступной базе.
- [x] 3.4 Проверить, что тесты, требующие лишь доступной базы, остаются под `require_postgres`, и что без базы прогон пропускает их, а не падает.

## 4. Защита обещания библиотеки

- [x] 4.1 Добавить тест, который пропускается, если в базе нет колонки `sku.added_at` или значений `sku.product_size_id` меньше порога: проверку «каждый пресет ускоряется» вернуть в условном виде.
- [x] 4.2 Проверить тестом, что пресеты разных рычагов ускоряются соответствующими индексами (`sku(product_id)`, `sku(product_size_id)`, `sku(added_at)`, `product(brand_id)`).

## 5. Демо-данные и документация

- [x] 5.1 Выполнить разовую заливку демо-базы по SQL из `design.md` (размеры до 50, бренды до 1000, товары до 20000, `sku.added_at` с разбросом, `analyze`) и проверить счётчики строк и число различных значений колонок запросом.
- [x] 5.2 Проверить на залитой базе, что каждый пресет из файла даёт значимое ускорение (ручной прогон или условный тест из раздела 4).
- [x] 5.3 Обновить `README.md`: раздел о демо-данных (форма и происхождение), о библиотеке пресетов в YAML и о запуске на произвольной базе.
- [x] 5.4 Обновить `pyproject.toml`: добавить `pyyaml` в зависимости и проверить, что синхронизация окружения проходит.
- [x] 5.5 Прогнать `uv run pytest -q` и `uv run ruff check src tests`, убедившись, что быстрые тесты зелёные и линтер чист.
