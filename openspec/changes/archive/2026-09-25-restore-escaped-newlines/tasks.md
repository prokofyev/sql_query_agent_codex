## 1. Восстановление переносов

- [x] 1.1 Добавить в `src/sql_query_agent/agent/llm.py` функцию восстановления переносов: посимвольный
  автомат состояний, замена `\n`/`\t` вне литералов и кавычек-идентификаторов (в том числе в
  комментариях); покрыть тестами вне литералов, внутри литералов, в кавычках-идентификаторах и в
  комментариях
- [x] 1.2 Применять восстановление к `SqlFix.sql` в `GigaChatAdvisor.propose_fix` только когда в ответе
  есть `\n` и нет настоящих переносов
- [x] 1.3 Проверить идемпотентность: ответ с настоящими переносами возвращается без изменений

## 2. Проверка

- [x] 2.1 Тесты адаптера модели: испорченный многострочный ответ, ответ с уже настоящими переносами,
  легитимный `\n` внутри литерала
- [x] 2.2 Тест подтверждения замен на испорченном тексте (`tests/test_run_report.py`): замены не пусты
- [x] 2.3 Прогнать `uv run pytest -q` и `uv run ruff check src tests`
