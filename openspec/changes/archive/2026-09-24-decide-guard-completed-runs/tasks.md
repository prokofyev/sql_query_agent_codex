## 1. Защита решения в сессии

- [x] 1.1 Добавить `SessionNotAwaitingDecisionError` и проверку ожидания решения в `SessionRunner.decide` (сессия есть, но точки остановки нет — ошибка; сессии нет — прежний `SessionNotFoundError`) и проверить тестом, что решение по завершённому прогону даёт эту ошибку.
- [x] 1.2 Обернуть проверку ожидания и `ainvoke` в блокировку по `thread_id` и проверить тестом с двумя одновременными решениями, что продолжение выполняется один раз.
- [x] 1.3 Добавить в `SessionRunner.report`/`decide` доступ к текущему этапу остановки и проверить тестом, что после принятия исправления решение с этапом `schema_fix` отклоняется, пока прогон стоит на `index_proposal`.

## 2. Контракт API

- [x] 2.1 Добавить код `not_awaiting_decision` и статус 409 в единый конверт ошибок (`api/errors.py`) и проверить тестом, что повторное решение по завершённому прогону даёт 409, а неизвестный прогон — по-прежнему 404.
- [x] 2.2 Отдать новую ошибку сессии в `POST /runs/{thread_id}/decision` и проверить тестом, что отчёт прогона после отклонённого решения не изменяется.
- [x] 2.3 Добавить обязательное поле этапа в `DecisionRequest` (лишние поля по-прежнему запрещены) и проверить тестами, что запрос без этапа даёт 422, а с чужим этапом — 409.
- [x] 2.4 Передавать этап показанного предложения из интерфейса в решении и проверить тестом `tests/test_ui.py`, что запрос решения содержит текущий этап, а существующие проверки решений обновлены.

## 3. Однократный учёт прогона

- [x] 3.1 Свести учёт прогона в метриках и журнале к одному признаку в `RunService._finalize` и проверить тестом, что повторный итоговый отчёт не удваивает счётчики и не добавляет значения в гистограмму.
- [x] 3.2 Проверить тестом, что решение, отклонённое как повторное, не меняет ни метрики, ни журнал.

## 4. Документация и проверка

- [x] 4.1 Описать в `README.md` поведение повторного и запоздалого решения (409 и код `not_awaiting_decision`).
- [x] 4.2 Прогнать `uv run pytest -q` и `uv run ruff check src tests`, убедившись, что быстрые тесты зелёные и линтер чист.
