## 1. Структурированный ответ модели

- [x] 1.1 Добавить модель замены и поле со списком замен в ответ исправления; у вложенной модели
  обязателен docstring (иначе GigaChat падает с `ValueError: Incorrect function or tool description`);
  проверить `uv run pytest -q tests/test_domain.py`
- [x] 1.2 Дополнить `FIX_SYSTEM_PROMPT`: модель возвращает и текст запроса, и список замен, и не
  заявляет замен, которых не сделала; проверить структуру ответа на опечатке в колонке и на
  неоднозначной колонке
- [x] 1.3 Обновить протокол `AdvisorModel.propose_fix` и вызовы в `agent/nodes.py`: исправление и
  заявленные замены попадают в состояние; обновить фикстуры в `tests/fakes.py`; проверить
  `uv run pytest -q tests/test_agent_graph.py`

## 2. Подтверждение замен по тексту

- [x] 2.1 Реализовать сравнение исходного и исправленного запросов по последовательности токенов SQL
  и сопоставление заявленных замен с фактическим различием; покрыть тестами в `tests/test_run_report.py`
- [x] 2.2 Проверить сценарий квалификации `brand_id → product.brand_id`: замена попадает в список,
  хотя старое имя осталось в тексте
- [x] 2.3 Проверить отбрасывание заявленной замены, которой нет в тексте, и пустой список замен при
  неизменённом запросе

## 3. Удаление эвристики

- [x] 3.1 Удалить `find_replacements`, `contains_identifier` и `IDENTIFIER_CHARS`; построение отчёта
  перевести на подтверждённые замены; проверить `uv run pytest -q tests/test_run_report.py`
- [x] 3.2 Переписать тесты на удалённые функции, сохранив покрытие сценариев «новое имя является
  частью старого» и «заменено и имя таблицы, и имя колонки»; проверить `uv run pytest -q`
- [x] 3.3 Проверить неизменность отчёта и интерфейса: `uv run pytest -q tests/test_api.py tests/test_ui.py`
  и `uv run ruff check src tests`
