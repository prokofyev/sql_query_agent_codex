## 1. Каркас проекта

- [x] 1.1 Создать src-layout (`src/sql_query_agent/`) и добавить в `pyproject.toml` зависимости: `langgraph`, `langgraph-checkpoint-postgres`, `langchain-gigachat`, `fastapi`, `uvicorn`, `nicegui`, `psycopg[binary,pool]`, `rapidfuzz`, `prometheus-client`, `pydantic-settings`, `structlog`; проверить, что `uv sync` проходит, а `requires-python >= 3.13` согласован с `.python-version`
- [x] 1.2 Настроить конфигурацию на `pydantic-settings` с вложенным `.env` (DSN базы `products`, креденшелы GigaChat, порог схожести, число повторов замера, таймаут) и добавить `.env.example`; проверить тестом, что настройки читаются из переменных окружения
- [x] 1.3 Настроить `pytest` (asyncio-mode auto, маркер `integration`), ruff (line-length 100) и `--run-llm` флаг для тестов с реальной моделью; проверить, что `uv run pytest -q` собирает тесты и `uv run ruff check src tests` проходит
- [x] 1.4 Реализовать создание схем `agent` и `metrics` в базе `products` (идемпотентно) и подключение `AsyncPostgresSaver` с заданием схемы через `search_path` на autocommit-соединении; проверить, что повторный вызов setup не падает, а таблицы `checkpoint_*` появляются в схеме `agent`

## 2. Инструмент проверки имён

- [x] 2.1 Реализовать чтение `information_schema` (таблицы и колонки схемы `public`) и сверку переданных имён с возвратом ненайденных; проверить тестом на фейковом каталоге, что существующие имена проходят, а отсутствующие попадают в результат
- [x] 2.2 Реализовать подбор похожих имён через `rapidfuzz` с порогом схожести и ограничением числа кандидатов; проверить тестом, что для `product_colr` находится `product_color`, а для имени без близких совпадений кандидатов нет
- [x] 2.3 Оформить `check_schema` как инструмент с простой схемой аргументов (`entities: list[{table: str, columns: list[str]}]`) без `Union`/`anyOf`; проверить тестом, что схема инструмента собирается и не содержит неподдерживаемых конструкций
- [x] 2.4 Проверить инструмент на реальной базе (маркер `integration`): существующие имена, опечатка в колонке, опечатка в имени таблицы; проверить, что для имени таблицы кандидаты подбираются без опоры на текст ошибок СУБД

## 3. Замер времени выполнения

- [x] 3.1 Реализовать замер в read-only транзакции с прогревом и N повторами, возвращая минимум, медиану, максимум и число повторов; проверить тестом с подставным исполнителем запросов, что медиана считается по повторам и не смещается единичным выбросом
- [x] 3.2 Реализовать ограничения безопасности замера: проверка, что ввод является одиночным запросом на чтение (`SELECT`/`WITH`), установка таймаута, сообщение о превышении времени; проверить тестами на пустом вводе, двух командах и команде изменения данных
- [x] 3.3 Проверить на реальной базе (маркер `integration`), что замер на запросе из библиотеки даёт стабильные величины, а повторный замер сопоставим с первым
- [x] 3.4 Реализовать `measure_query` как инструмент; проверить тестом, что он возвращает структурированный результат и текст ошибки при неудаче, не возбуждая исключение наружу

## 4. Применение индекса

- [x] 4.1 Реализовать применение индекса: `BEGIN` → `CREATE INDEX` → повторный замер → `ROLLBACK` с гарантированным откатом при ошибке; проверить на реальной базе (маркер `integration`), что после прогона индекс отсутствует в `pg_indexes`
- [x] 4.2 Реализовать вычисление разницы до/после с порогом значимости и тремя исходами: ускорение, отсутствие значимого ускорения, незначительное ускорение; проверить тестами на подставных величинах все три исхода
- [x] 4.3 Проверить на реальной базе (маркер `integration`) оба эталонных случая: запрос, который ускоряется (точечный фильтр), и запрос, который не ускоряется (низкоселективная колонка); проверить, что второй случай возвращается как «без ускорения», а не как ошибка
- [x] 4.4 Реализовать `apply_index` как инструмент, возвращающий обе величины и разницу; проверить тестом, что при ошибке создания индекса транзакция откатывается и возвращается текст ошибки
- [x] 4.5 Зафиксировать библиотеку из пяти предустановленных запросов с описанием ожидаемого эффекта; проверить тестом, что в наборе есть минимум три ускоряемых и минимум два неускоряемых по разным причинам

## 5. Граф агента

- [x] 5.1 Описать состояние графа (исходный и текущий запрос, результат проверки имён, замеры, предложение, решения пользователя, предупреждения) и собрать `StateGraph` с узлами извлечения имён, проверки, исправления, замера, предложения индекса и применения
- [x] 5.2 Реализовать узел извлечения имён через `bind_tools([check_schema])` и обработку возвращённого результата; проверить тестом с подставной моделью, что аргументы инструмента передаются как `entities`
- [x] 5.3 Реализовать ветку предупреждения: если модель не вызвала инструмент, обработка продолжается, а в состоянии появляется предупреждение о пропущенной проверке; проверить тестом с подставной моделью, вернувшей текст без вызова инструмента
- [x] 5.4 Реализовать однократное исправление запроса моделью по кандидатам без автоматического повторного цикла; проверить тестом, что после исправления повторная проверка имён не запускается
- [x] 5.5 Реализовать две точки `interrupt()` (подтверждение исправления и подтверждение индекса) и обработку `Command(resume=...)` для решений «принять»/«отказаться»; проверить тестом, что при отказе граф завершается, а при принятии продолжается
- [x] 5.6 Обеспечить идемпотентность узлов с `interrupt()` (кэш результата замера и извлечённых имён в состоянии), поскольку LangGraph перезапускает узел при резюме; проверить тестом, что подставная модель вызывается один раз, несмотря на резюме
- [x] 5.7 Реализовать предложение индекса моделью с обоснованием на основе плана и замеров, исключив переформулировку запроса; проверить тестом с подставной моделью, что результат содержит команду создания индекса и не содержит переписанный запрос

## 6. API

- [x] 6.1 Реализовать создание сессии и запуск обработки с привязкой `thread_id`, включая случай остановки на `interrupt`; проверить тестом, что ответ содержит признак ожидания решения и данные предложения
- [x] 6.2 Реализовать приём решения пользователя (принятие или отказ) и продолжение графа по `thread_id`; проверить тестом полный цикл «запуск → решение → завершение» на подставной модели
- [x] 6.3 Реализовать валидацию входящего запроса на уровне API (пустой ввод, несколько команд, не-`SELECT`); проверить тестами коды ответов и сообщения для каждого случая
- [x] 6.4 Реализовать эндпоинт `/metrics` и эндпоинт истории прогонов; проверить тестом, что метрики отдаются в формате Prometheus и не содержат текста запроса в метках

## 7. Метрики и журнал

- [x] 7.1 Реализовать запись audit-строки прогона в схему `metrics` (запрос, результат проверки имён, замеры, разница, решения, время завершения, признак отсутствия ускорения); проверить на реальной базе (маркер `integration`), что запись содержит все поля
- [x] 7.2 Реализовать запись audit-строки для прерванных прогонов (отказ от исправления или от индекса) с указанием этапа остановки; проверить тестом оба сценария отказа
- [x] 7.3 Реализовать метрики Prometheus: число прогонов, число применённых индексов, число случаев без ускорения, гистограмма длительности замеров, счётчики вызовов GigaChat; проверить тестом, что метки не содержат значений высокой кардинальности
- [x] 7.4 Обернуть запись в журнал так, чтобы ошибка записи не прерывала обработку; проверить тестом с подставным журналом, возвращающим ошибку, что результат пользователю всё равно возвращается

## 8. Веб-интерфейс

- [x] 8.1 Реализовать страницу NiceGUI с полем ввода и списком пяти предустановленных запросов; проверить тестом, что выбор запроса подставляет текст в поле
- [x] 8.2 Реализовать внутрипроцессный клиент API через `httpx.ASGITransport` и подключить его к UI, чтобы контракт был единым; проверить тестом, что клиент работает против тестового приложения
- [x] 8.3 Реализовать отображение предложения об исправлении (исправленный запрос, список замен) с действиями «принять»/«отказаться»; проверить тестом, что после отказа поле ввода снова доступно
- [x] 8.4 Реализовать отображение предложения об индексе (команда, обоснование) и результата замера (до, после, разница) с равнозначным показом случая «без ускорения»; проверить тестом обе ветки результата
- [x] 8.5 Реализовать индикацию длительной операции при создании индекса и блокировку повторной отправки во время обработки; проверить тестом, что во время обработки повторная отправка недоступна
- [x] 8.6 Реализовать отображение предупреждения о пропущенной проверке имён; проверить тестом, что предупреждение показывается и успешная проверка при этом не отображается

## 9. Сквозная проверка

- [x] 9.1 Собрать приложение (API и UI в одном процессе) и проверить запуск на локальном PostgreSQL с базой `products`
- [x] 9.2 Прогнать сквозной сценарий на подставных модели и базе: запрос с опечаткой → исправление → принятие → замер → принятие индекса → разница → запись в журнал
- [x] 9.3 Прогнать сквозной сценарий с реальной моделью GigaChat за флагом `--run-llm` на запросе с опечаткой; убедиться, что предложение об исправлении содержит замену, найденную по `information_schema`
- [x] 9.4 Прогнать на реальной базе (маркер `integration`) полный цикл для одного ускоряемого и одного неускоряемого предустановленного запроса; проверить, что первый сообщает ускорение, второй — отсутствие ускорения с причиной, и что индекс после прогона отсутствует
- [x] 9.5 Проверить, что после всех прогонов схема `public` не изменилась: число строк и список индексов в таблицах демо-данных совпадают с исходными
