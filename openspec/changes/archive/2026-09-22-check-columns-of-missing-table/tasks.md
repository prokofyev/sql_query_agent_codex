## 1. Проверка колонок отсутствующей таблицы

- [x] 1.1 Изменить ветку отсутствующей таблицы в `check_entities` (`src/sql_query_agent/tools/schema_check.py`): подобрать таблицы-кандидаты, собрать их колонки и проверить по этому пулу каждую колонку сущности; проверить юнит-тестом, что для пары «таблица `sku2` + колонка `product_id2`» в ненайденных оказываются оба имени
- [x] 1.2 Проверить юнит-тестом, что отсутствующая таблица без кандидатов оставляет единственным ненайденным именем саму таблицу, а её колонки ненайденными не объявляются
- [x] 1.3 Проверить юнит-тестом, что если колонка отсутствующей таблицы есть среди колонок таблиц-кандидатов, она ненайденной не считается
- [x] 1.4 Проверить юнит-тестом, что колонка отсутствующей таблицы, для которой кандидатов нет, попадает в ненайденные с пустым списком кандидатов, и `can_fix_all` для такого результата ложно
- [x] 1.5 Проверить, что существующие сценарии проверки имён не изменились: тесты `tests/test_schema_check.py` и `tests/test_check_schema_integration.py` проходят без правок ожиданий

## 2. Сопоставление выполненных замен

- [x] 2.1 Реализовать в `src/sql_query_agent/run/report.py` проверку вхождения имени отдельным токеном (соседние символы не являются символами идентификатора) и применить её к поиску нового имени в исправленном запросе и старого имени в исходном; проверить юнит-тестом, что замена `sku2` → `sku` и `product_id2` → `product_id` попадает в список замен
- [x] 2.2 Проверить юнит-тестом, что `sku` внутри `sku2` не считается присутствием нового имени и не мешает определить замену, а внутри `skus` не создаёт ложную замену
- [x] 2.3 Проверить юнит-тестом, что при совпадении исправленного и исходного запросов список замен пуст, и что для двух заменённых имён список содержит обе замены
- [x] 2.4 Проверить сквозным тестом на подставных модели и базе, что для `select * from sku2 where product_id2 = 42` отчёт содержит обе замены, а прогон не падает на замере

## 3. Сквозная проверка

- [x] 3.1 Прогнать сквозной сценарий с опечаткой в таблице и колонке на реальной базе (маркер `integration`): проверить, что проверка имён возвращает оба ненайденных имени, а кандидат колонки — из колонок таблицы `sku`
- [x] 3.2 Прогнать сквозной сценарий с реальной моделью за флагом `--run-llm`: запрос с опечаткой в таблице и колонке приводит к предложению исправления, в котором заменены оба имени
