# schema-validation Specification

## Purpose

Проверяет, что имена таблиц и колонок в запросе существуют в базе, подбирает похожие имена при
опечатках и позволяет пользователю подтвердить исправленный запрос перед дальнейшей обработкой.

## Requirements

### Requirement: Извлечение идентификаторов моделью

Система SHALL извлекать имена таблиц и колонок из текста запроса с помощью языковой модели, передавая
их инструменту проверки как структурированный список «таблица — колонки». Разбор SQL штатными
средствами СУБД для этой цели MUST NOT использоваться.

#### Scenario: Имена извлекаются из запроса
- **WHEN** пользователь отправляет корректный запрос на чтение
- **THEN** модель возвращает список таблиц и относящихся к ним колонок
- **AND** имена передаются инструменту проверки в том виде, в каком они записаны в запросе

#### Scenario: Опечатки не исправляются до проверки
- **WHEN** запрос содержит имя с опечаткой
- **THEN** модели запрещено исправлять его при извлечении
- **AND** инструменту передаётся имя ровно так, как оно написано в запросе

### Requirement: Сверка имён со схемой базы

Инструмент проверки SHALL читать метаданные схемы из `information_schema` целевой базы и определять,
какие из переданных имён отсутствуют. Подсказки СУБД о похожих именах, возвращаемые в тексте ошибок,
MUST NOT использоваться.

#### Scenario: Все имена существуют
- **WHEN** все переданные имена найдены в схеме
- **THEN** инструмент сообщает, что проверка пройдена
- **AND** обработка продолжается без исправлений

#### Scenario: Имя отсутствует в схеме
- **WHEN** хотя бы одно переданное имя не найдено в схеме
- **THEN** инструмент возвращает это имя среди ненайденных
- **AND** указывает, к какой таблице оно относится

### Requirement: Подбор похожих имён

Для каждого ненайденного имени инструмент SHALL подбирать похожие существующие имена и возвращать их
вместе с оценкой схожести. Поиск MUST выполняться по данным, полученным из `information_schema`.

#### Scenario: Опечатка в имени колонки
- **WHEN** инструмент получает имя колонки, которого нет в таблице
- **THEN** он возвращает существующие имена этой таблицы, похожие на введённое
- **AND** для каждого кандидата указана оценка схожести

#### Scenario: Похожих имён не найдено
- **WHEN** для ненайденного имени не найдено ни одного кандидата выше порога схожести
- **THEN** инструмент сообщает, что подходящих замен нет
- **AND** пользователю предлагается исправить запрос вручную

### Requirement: Однократное исправление запроса

При наличии ненайденных имён модель SHALL один раз исправить исходный запрос, опираясь на
предложенных кандидатов. Повторная проверка исправленного запроса MUST NOT запускаться автоматически.

#### Scenario: Модель исправляет опечатки
- **WHEN** инструмент вернул ненайденные имена и кандидатов на замену
- **THEN** модель возвращает исправленный текст запроса
- **AND** показывает пользователю, какие имена были замены

#### Scenario: Исправление не повторяется автоматически
- **WHEN** модель выдала исправленный запрос
- **THEN** система не запускает ещё один цикл исправления
- **AND** решение передаётся пользователю

### Requirement: Подтверждение исправления пользователем

Система SHALL приостанавливать обработку и запрашивать у пользователя решение по исправленному
запросу. При отказе обработка MUST завершаться, а интерфейс MUST возвращать пользователя к вводу
нового запроса.

#### Scenario: Пользователь принимает исправление
- **WHEN** пользователь подтверждает исправленный запрос
- **THEN** исправленный запрос становится текущим
- **AND** обработка продолжается со следующего этапа

#### Scenario: Пользователь отказывается от исправления
- **WHEN** пользователь отклоняет исправленный запрос
- **THEN** обработка завершается
- **AND** интерфейс показывает поле ввода для нового запроса

### Requirement: Предупреждение при отсутствии вызова инструмента

Если модель не вызвала инструмент проверки имён, система SHALL продолжать обработку и SHALL сообщать
пользователю предупреждение о том, что проверка имён не выполнялась.

#### Scenario: Модель не вызвала инструмент
- **WHEN** модель не сформировала вызов инструмента проверки
- **THEN** система не прерывает обработку
- **AND** показывает пользователю предупреждение о пропущенной проверке имён
